#include<bits/stdc++.h>
using namespace std;
void pre_input(string s, vector<string>& nums)
{
	for (int i = 0; i < s.size(); i++)
	{
		int j = i;
		while (j < s.size() && s[j] != ',')
		{
			j++;
			if (s[j] == ',')
			{
				string tmp=
			}
		}
	}
}
int main()
{
	string input;
	while (getline(cin, input))
	{

	}
	return 0;
}